[project]
name = "chatesp-tools"
version = "0.1.0"
description = "Pinned host tools for ChatESP firmware development"
requires-python = ">=3.13,<3.14"
dependencies = [
  "pip==26.1.2",
  "platformio==6.1.19",
]

[tool.uv]
package = false
exclude-newer = "2 weeks"
required-version = ">=0.11.32"
